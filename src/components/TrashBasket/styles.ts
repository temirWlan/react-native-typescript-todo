import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	img: {
		width: 90,
		height: 90,
		backgroundColor: '#6482d9',
		borderRadius: this.width / 2
	}
});

export default styles;